import cloudinaryConfig from '@/api/cloudinaryConfig';
import axios from 'axios';

const deleteImageCloudinary = async(id) => {

    cloudinaryConfig();

    try {
        
    } catch (error) {
        
    }

};

export default deleteImageCloudinary;