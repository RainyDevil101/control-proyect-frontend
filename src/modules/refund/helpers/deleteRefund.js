import deleteImageCloudinary from "./deleteImageCloudinary";

const deleteRefund = async(id) => {

    if (!id || id.length === 0) return;

    try {

        // await deleteImageCloudinary();
        
    } catch (error) {
        
    }

};

export default deleteRefund;